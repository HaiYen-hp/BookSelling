import { Dimensions } from "react-native";
const colors = {
  main: "#E7E25B",
  mainText: "#000000",
  text: "#000000",
};
const { width, height } = Dimensions.get("screen");
export default { colors, width, height };
